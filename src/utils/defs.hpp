#pragma once

/// C++ includes
#include <map>

/// Custom includes
#include "core/factoradic.hpp"

typedef map<string, factoradic> memory;
typedef memory::const_iterator caddress;
typedef memory::iterator address;
